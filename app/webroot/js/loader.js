	// var cl = new CanvasLoader('canvasloader-container');
	// cl.setColor('#047f8d');
	// cl.setShape('spiral');
	// cl.setDiameter(100);
	// cl.show();
	// $('#loading').show();

	// var loaderObj = document.getElementById("canvasloader-container");
	// loaderObj.style.zIndex = 999;
	// loaderObj.style.position = "absolute";
	// loaderObj.style["top"] = ($(window).height() / 2 - cl.getDiameter() / 2) + "px";
	// loaderObj.style["left"] = ($(window).width() / 2 - cl.getDiameter() / 2) + "px";

	// $(function() {
	// 	cl.hide();
	// 	$('#loading').hide();
	// });